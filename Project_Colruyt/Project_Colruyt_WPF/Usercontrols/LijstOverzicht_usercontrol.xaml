<UserControl x:Class="Project_Colruyt_WPF.Usercontrols.LijstOverzicht_usercontrol"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project_Colruyt_WPF.Usercontrols"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             FontFamily="{materialDesign:MaterialDesignFont}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding Lijst}" Grid.Row="0" Margin="30 20" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectedItem="{Binding SelectItem}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Lijstnaam" Binding="{Binding Lijstnaam}"/>
                <DataGridTextColumn Header="Datum" Binding="{Binding Datum,  StringFormat=dd-MM-yyyy}"/>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Background="Transparent">
                            <Button Margin="20 0 0 0" Command="{Binding}" CommandParameter="Verwijder" 
                                Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}">
                                <materialDesign:PackIcon Kind="TrashCanOutline" Height="24" Width="24"/>
                            </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTemplateColumn.CellStyle>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <!--
        <ScrollViewer Grid.Row="0" materialDesign:ScrollViewerAssist.IsAutoHideEnabled="True" Margin="30 20">
            <ListView x:Name="LVOverzichtLijsten" ItemsSource="{Binding Lijst}">
                
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="460">
                            <TextBlock FontSize="18" FontWeight="SemiBold" Text="{Binding Lijstnaam}"  VerticalAlignment="Center" HorizontalAlignment="Left" />
                        </GridViewColumn>

                        <GridViewColumn Width="auto" >
                            <TextBlock Text="{Binding Datum}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </GridViewColumn>

                        <GridViewColumn Width="auto" >
                            <Button
                                Margin="20 0 0 0"
                                Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}">
                                <materialDesign:PackIcon Kind="TrashCanOutline" Height="24" Width="24" />
                            </Button>
                        </GridViewColumn>

                    </GridView>
                </ListView.View>
            </ListView>
        </ScrollViewer>
-->
        <Button
            Style="{StaticResource MaterialDesignFloatingActionLightButton}"
            Grid.Row="1" HorizontalAlignment="Right"
            Margin="40 20" Command="{Binding}" CommandParameter="VoegToe">
            <materialDesign:PackIcon
                Kind="Plus"
                Height="26"
                Width="26"/>
        </Button>
        <Button  Style="{StaticResource MaterialDesignFloatingActionLightButton}"
            Grid.Row="1" HorizontalAlignment="Right"
            Margin="0,20.4,688,19.6" Command="{Binding}" CommandParameter="Voegtoe" Content="Open">
           
        </Button>

        <Button  Style="{StaticResource MaterialDesignFloatingActionLightButton}"
            Grid.Row="1" HorizontalAlignment="Right"
            Margin="0,20.4,365,19.6" Command="{Binding}" CommandParameter="Verwijder" Content="Remove">

        </Button>

    </Grid>
</UserControl>
