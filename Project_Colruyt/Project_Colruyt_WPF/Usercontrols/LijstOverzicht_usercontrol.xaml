<UserControl x:Class="Project_Colruyt_WPF.Usercontrols.LijstOverzicht_usercontrol"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project_Colruyt_WPF.Usercontrols"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             FontFamily="{materialDesign:MaterialDesignFont}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer Grid.Row="2" materialDesign:ScrollViewerAssist.IsAutoHideEnabled="True" Margin="30 20">
            <ListView x:Name="LVOverzichtLijsten">
                
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="460">
                            <TextBlock FontSize="18" FontWeight="SemiBold" Text="Winkellijst Fred"  VerticalAlignment="Center" HorizontalAlignment="Left" />
                        </GridViewColumn>

                        <GridViewColumn >
                            <TextBlock Text="12/11/2020" VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </GridViewColumn>

                        <GridViewColumn >
                            <Button
                                Margin="20 0 0 0"
                                Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}">
                                <materialDesign:PackIcon Kind="TrashCanOutline" Height="24" Width="24" />
                            </Button>
                        </GridViewColumn>

                    </GridView>
                </ListView.View>
            </ListView>
        </ScrollViewer>
    </Grid>
</UserControl>
